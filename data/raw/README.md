Store immutable original source downloads here. Large/raw inputs are ignored by
Git. Record URL, retrieval date, release/vintage, geography vintage, license and
SHA-256 in a source manifest before processing. Keep every used release available
locally; changing an input should create a new run, not replace old results.
