USDA tied-percentile closure red run — 2026-09-13

Command:
PYTHONPATH=src .venv/bin/python -m unittest tests.test_usda_bdd -v

Before the change, `percentile_scores([5, 5, 10, 20, 20])` returned
`[0.0, 0.25, 0.5, 0.75, 1.0]` rather than the average-rank result
`[0.125, 0.125, 0.5, 0.875, 0.875]`. A permutation of the same ties also
produced different scores for equal values. The new cohort-metadata scenario
failed because the calculation did not report a declared percentile cohort,
indicator observation period, or indicator codes.
