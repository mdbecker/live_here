The temperature pipeline BDD test was run before the adapter implementation.

Command:
PYTHONPATH=src .venv/bin/python -m unittest tests.test_temperature_pipeline_bdd -v

Observed failure:
ValueError: Factors awaiting implementation: ['heat']

The real-data acceptance scenario was also run before downloads and failed on
the missing NOAA archive. Both failures were resolved only after the source
adapter and gather script were implemented.
