OEWS closure BDD red phase — September 13, 2026

Command:
  PYTHONPATH=src .venv/bin/python -m unittest tests.test_oews_closure_bdd -v

Result before implementation: FAILED (5 scenarios)

- `occupation_share` did not exist, so the all-occupations denominator and
  suppression-completeness scenarios errored.
- `calculate_trades` did not accept metro, county-CBSA, or industry-share
  inputs, so recovered local adjustment behavior errored.
- Sparse XLSX parsing lost the `OCC_CODE` column because it appended cells
  instead of respecting their A1 references.
- A missing industry share still produced a state-adjusted value, which would
  silently treat the selected share as one.

The command output recorded 4 errors and 1 failure. This is prospective
red-to-green evidence for C1; it does not replace the older narrative log.
