Only one Python script is explicitly named in the available project conversation history:

- `fix_candidate_source_backed_fields_and_rerun(1).py` — referenced in **“Workflow Refactor and Politics Factor”** on June 10, 2026. It appears to correct candidate-county fields using real source data and regenerate the ranking workbook.

Other Python scripts are mentioned but not named:

- Scripts used to populate source-backed factors
- Scripts used to add candidate counties
- Code for the politics factor
- Scripts used to produce or update the county-ranking spreadsheet
- Earlier scripts recommended as sources for the planned V1 Python project

So, from the context currently available, I can verify exactly **one filename**. The June 10 conversation apparently contained a more detailed script-by-script inventory, but that portion is not preserved in the visible project summary.