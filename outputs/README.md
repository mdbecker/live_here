Each execution writes a separate directory containing counties.csv, factors.csv,
rankings.csv, coverage.json, and run-manifest.json. Generated files are ignored by
Git. The pipeline refuses to replace a nonempty run directory. Demo results are
synthetic; research results are partial until the V1 completion gates are met.
